app.controller('HomeController', ['$scope', function($scope) {
    $scope.test = "test home controller";
}]);

app.controller('ConfigureController', ['$scope', function($scope) {
    $scope.test = "test configure controller";
}]);